# When deploying the client separately from the server,
# set this to your backend's public URL (no trailing slash).
# In local dev, leave it unset — Vite's proxy handles /api → localhost:5000.
VITE_API_URL=https://your-backend-domain.com/api
